# docker-compose.yml
services:
  forumcinemas-reporter:
    image: mcr.microsoft.com/playwright:v1.55.0-jammy
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    command: sh -c "npm install && npm start"